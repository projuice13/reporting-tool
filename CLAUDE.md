# Project guidance for Claude

## Git workflow

- Push all changes directly to the `main` branch. The user has standing
  authorization for this — no need to ask before pushing to `main`, and there's
  no need to open a pull request unless the user explicitly asks for one.
